n = int(input())
max_digit = -1 #1
while n > 0:
    digit = n % 10
    if digit % 3 == 0: #3
        if digit > max_digit: #2
            max_digit = digit #11
    n = n // 10 #5
if max_digit == -1: #4
    print('NO')
else:
    print(max_digit)
